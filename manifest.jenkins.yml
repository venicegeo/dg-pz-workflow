applications:
- name: pz-workflow
  host: pz-workflow
  path: pz-workflow.jar
  command: chmod 755 pz-workflow && ./pz-workflow
  buildpack: https://github.com/cloudfoundry/java-buildpack.git 
  memory: 512M
  instances: 1
  timeout: 180
  services:
#      - pz-elasticsearch
      - pz-kafka
  env:
    LOGGER_INDEX: piazzalogger
